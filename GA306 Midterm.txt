GA306 MidtermJerryd PerryShort Answer1. Function2. A default argument3. A *arg tells python to pack all the variables passed to the function as a tuple, **kwargs tells the interpreter to pack all key-value pairs passed into the function into a dictionary4. A. from 0 to upper_limit -1         B. from lower_bound to upper_llimit-1        C.  from lower bound to upper_limit-1, using step value5. If, Else, and Elif6. List comprehensions condense code.  Allows new sequences using a combo for the for statement and conditional statements